# Railway configuration for SuperNode backend
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "supernode"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
